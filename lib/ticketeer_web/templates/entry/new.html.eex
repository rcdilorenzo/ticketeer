<h2>New Entry</h2>

<%= render "form.html", Map.put(assigns, :action, student_entry_path(@conn, :create, @student)) %>

<span><%= link "Back", to: student_entry_path(@conn, :index, @student) %></span>
